# Stellar Network Configuration
# Opções: testnet, futurenet, mainnet
STELLAR_NETWORK=testnet

# Stellar Identity/Source
# Use o nome da identity configurada no Stellar CLI
# Para criar uma nova: stellar keys generate YOUR_IDENTITY_NAME --network testnet
STELLAR_SOURCE=alice

# Admin Address
# Endereço Stellar (G...) que será o administrador do contrato
# Exemplo: GABC123XYZ456...
ADMIN_ADDRESS=

# Contract ID (preenchido automaticamente após deploy)
CONTRACT_ID=

# WASM Hash (preenchido automaticamente após install)
WASM_HASH=
